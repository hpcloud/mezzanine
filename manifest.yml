applications:
- name: mezzanine
  docker_image: stackatoapps/mezzanine:latest
  mem: 128M
  services:
    ${name}-db:
      type: mysql
  stackato:
    env:
      DJANGO_SETTINGS_MODULE:
        default: settings
    ignores:
    - .git
    - .pyc
    - .db
    min_version:
      client: 1.4.3
