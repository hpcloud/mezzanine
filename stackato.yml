name: mezzanine

mem: 64M

env:
  DJANGO_SETTINGS_MODULE: settings
  
requirements:
  pypm:
    - psycopg2
    - django<=1.3.999
    - pillow
  
services:
  postgresql-mezzanine: postgresql

ignores: 
  - '*.pyc'
  - 'mezzanine.db'

hooks:
  post-staging:
    - python manage.py syncdb --noinput
